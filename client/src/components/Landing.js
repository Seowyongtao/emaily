import React from "react";

const Landing = () => {
  return(
    <div style={{textAlign: "center"}}>
      <h1>Emaily !</h1>
      Here is the landing page !
    </div>
  )
};

export default Landing;
